"use client";
import { FormEvent, useState } from "react";

export default function CompanyForm({ username }: { username: string }) {
  const [error, setError] = useState(""); const [pending, setPending] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); setError(""); const data = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/company-profile", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); const result = await response.json(); if (!response.ok) { setError(result.error); setPending(false); return; } window.location.assign("/"); }
  return <main className="onboarding-shell"><section className="onboarding-card"><div className="onboarding-top"><span>QT</span><div><small>АККАУНТ ТЕНДЕРЩИКА</small><strong>{username}</strong></div></div><p className="eyebrow">ПЕРСОНАЛИЗАЦИЯ РАДАРА</p><h1>Расскажите о компании</h1><p className="onboarding-intro">Эти данные станут фильтрами допуска и основой рейтинга тендеров. Их можно будет изменить позже.</p><form className="company-form" onSubmit={submit}><label className="wide">Название компании<input name="companyName" required /></label><label>БИН<input name="bin" inputMode="numeric" pattern="\d{12}" maxLength={12} required /></label><label>Опыт, лет<input name="experienceYears" type="number" min="0" max="100" required /></label><label className="wide">Регионы работы<textarea name="regions" placeholder="Например: Кызылординская и Туркестанская области" required /></label><label className="wide">Виды строительных работ<textarea name="workCategories" placeholder="Капремонт, инженерные сети, благоустройство…" required /></label><label className="wide">Лицензии и категории<textarea name="licenses" placeholder="Номер, категория и виды лицензируемых работ" required /></label><label>Количество сотрудников<input name="employeeCount" type="number" min="1" required /></label><label>Минимальный бюджет, ₸<input name="minBudget" type="number" min="0" step="1000000" required /></label><label>Максимальный бюджет, ₸<input name="maxBudget" type="number" min="1" step="1000000" required /></label>{error && <p className="login-error wide" role="alert">{error}</p>}<button className="wide" disabled={pending}>{pending ? "Сохраняем…" : "Сохранить и открыть радар"}</button></form></section></main>;
}
